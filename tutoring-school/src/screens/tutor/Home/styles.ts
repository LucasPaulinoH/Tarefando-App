import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  schoolCard: {
    display: "flex",
    flexDirection: "row",
    alignItems: "center",
    gap: 20,
  },

  schoolCardFirstHalf: {
    display: "flex",
    flexDirection: "row",
    alignItems: "center",
    gap: 10,
  },

  schoolAvatar: {
    borderRadius: 5,
  },
});

export default styles;
